<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Database Schema</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="schema.css" rel="stylesheet">
</head>
<body class="py-4">
<div class="container">
  <h1>Database Schema Documentation</h1>
  
  <p class="lead mb-4">
    <strong>Total Tables:</strong> <span class="badge bg-primary">{{ tables | len }}</span>
  </p>

  <div v-for="table in tables">
    <h2>{{ table.name | upper }} <span class="badge bg-info text-dark">{{ table.columns | len }} columns</span></h2>
    
    <div v-if="table.comment">
      <p class="mb-3"><strong>Description:</strong> {{ table.comment }}</p>
    </div>

    <h3>Columns</h3>
    <table class="table table-striped table-hover mb-4">
      <thead class="table-primary">
        <tr>
          <th>Name</th>
          <th>SQL Type</th>
          <th>Data Type</th>
          <th>Constraints</th>
          <th>Description</th>
          <th>Enum Values</th>
        </tr>
      </thead>
      <tbody>
        <tr v-for="column in table.columns">
          <td><strong>{{ column.name }}</strong></td>
          <td><span class="datatype">{{ column.type }}</span></td>
          <td><span class="datatype">{{ column.datatype }}</span></td>
          <td class="small-text">
            <div>
              <div v-if="column.key == 'PRI'" class="mb-2"><span class="key-badge key-primary">PRIMARY KEY</span></div>
              <div v-else-if="column.key == 'UNI'" class="mb-2"><span class="key-badge key-unique">UNIQUE</span></div>
              <div v-else-if="column.key == 'MUL'" class="mb-2"><span class="key-badge key-index">INDEXED</span></div>
            </div>
            <div v-if="column.nullable" class="text-muted">Nullable</div>
            <div v-if="column.default" class="text-muted">Default: <code>{{ column.default }}</code></div>
          </td>
          <td>
            <div>{{ column.comment }}</div>
            <div v-if="column.attributes" class="text-light mt-2">
              <div v-for="(val, key) in column.attributes">• {{ key }}: {{ val }}</div>
            </div>
          </td>
          <td>
            <div v-if="column.enum_values">
              <div class="enum-values">
                <div v-for="val in column.enum_values">• {{ val }}</div>
              </div>
            </div>
            <span v-else class="text-light">-</span>
          </td>
        </tr>
      </tbody>
    </table>

    <div v-if="table.indexes">
      <h3>Indexes</h3>
      <table class="table table-striped table-hover mb-4">
        <thead class="table-primary">
          <tr>
            <th>Index Name</th>
            <th>Columns</th>
            <th>Type</th>
            <th>Properties</th>
          </tr>
        </thead>
        <tbody>
          <tr v-for="index in table.indexes">
            <td>
              <div v-if="index.name">
                <strong>{{ index.name }}</strong>
              </div>
              <div v-else>
                <strong class="text-muted">auto-generated</strong>
              </div>
            </td>
            <td>
              <div>
                <span v-for="(i, col) in index.columns">
                  <div v-if="i > 0" class="mt-2"></div>
                  <code>{{ col }}</code>
                </span>
              </div>
            </td>
            <td>
              <div>
                <span v-if="index.primary" class="key-badge key-primary">PRIMARY KEY</span>
                <span v-else-if="index.unique" class="key-badge key-unique">UNIQUE</span>
                <span v-else class="key-badge key-index">INDEX</span>
              </div>
            </td>
            <td class="small-text">
              <div v-if="index.primary">Enforces uniqueness and not-null</div>
              <div v-else-if="index.unique">Ensures all values are unique</div>
              <div v-else>Improves query performance</div>
              <div class="text-light mt-2">
                <span v-if="index.columns">Covers {{ index.columns | len }}<span v-if="index.columns | len == 1"> column</span><span v-else> columns</span></span>
              </div>
            </td>
          </tr>
        </tbody>
      </table>

      <div class="info-box primary">
        <h4>Index Summary</h4>
        <ul>
          <li>Total indexes: <strong>{{ table.indexes | len }}</strong></li>
          <li>
            Primary keys:
<template v-for="(i, idx) in table.indexes" v-if="idx.primary">
   <span class="badge badge-info">{{ idx.name | default("(auto)") }} {{ idx.columns }}</span>
</template>
          </li>
          <li>

            Performance indexes:
<template v-for="(i, idx) in table.indexes">
   <span v-if="idx.primary"></span>
   <span v-else>
{{ idx.name }} {{ idx.columns }}
   </span>
</template>

          </li>
        </ul>
      </div>
    </div>
  </div>

  <div class="section-divider">
    <h2>Schema Statistics</h2>
    
    <div class="stats-grid">
      <div class="stat-card">
        <h4>Tables</h4>
        <p class="stat-value">{{ tables | len }}</p>
      </div>
      
      <div class="stat-card danger">
        <h4>Total Columns</h4>
        <p class="stat-value">
          <span v-for="table in tables">{{ table.columns | len }}</span>
        </p>
      </div>
      
      <div class="stat-card success">
        <h4>Total Indexes</h4>
        <p class="stat-value">
          <span v-for="table in tables">{{ table.indexes | len }}</span>
        </p>
      </div>
    </div>

    <h3>Table Details</h3>
    <table class="table table-striped table-hover">
      <thead class="table-primary">
        <tr>
          <th>Table</th>
          <th>Description</th>
          <th class="text-center">Columns</th>
          <th class="text-center">Primary Keys</th>
          <th class="text-center">Indexes</th>
        </tr>
      </thead>
      <tbody>
        <tr v-for="table in tables">
          <td><strong>{{ table.name }}</strong></td>
          <td>{{ table.comment }}</td>
          <td class="text-center">{{ table.columns | len }}</td>
          <td class="text-center">
            <span v-for="(col, i) in table.columns" v-if="col.key == 'PRI'">
              <span v-if="i > 0">, </span>{{ col.name }}
            </span>
          </td>
          <td class="text-center">{{ table.indexes | len }}</td>
        </tr>
      </tbody>
    </table>

    <h3 class="mt-5">Column Type Distribution</h3>
    <p class="text-muted">Aggregated data type usage across all tables in the schema.</p>
    <table class="table table-striped table-hover">
      <thead class="table-primary">
        <tr>
          <th>Data Type</th>
          <th>SQL Type</th>
          <th class="text-center">Usage Count</th>
          <th>Tables Using This Type</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><span class="datatype">text</span></td>
          <td>TEXT</td>
          <td class="text-center"><strong>12</strong></td>
          <td>event, event_log, migrations</td>
        </tr>
        <tr>
          <td><span class="datatype">integer</span></td>
          <td>INTEGER</td>
          <td class="text-center"><strong>7</strong></td>
          <td>event, event_log, migrations</td>
        </tr>
        <tr>
          <td><span class="datatype">timestamp</span></td>
          <td>TIMESTAMP</td>
          <td class="text-center"><strong>5</strong></td>
          <td>event, event_log</td>
        </tr>
        <tr>
          <td><span class="datatype">enum</span></td>
          <td>TEXT</td>
          <td class="text-center"><strong>1</strong></td>
          <td>event</td>
        </tr>
      </tbody>
    </table>

    <h3 class="mt-5">All Columns by Table</h3>
    <p class="text-muted">Complete list of all columns across all tables with full details.</p>
    <table class="table table-striped table-hover">
      <thead class="table-primary">
        <tr>
          <th>Table</th>
          <th>Column Name</th>
          <th>SQL Type</th>
          <th>Data Type</th>
          <th>Constraints</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <template v-for="table in tables">
          <tr v-for="col in table.columns">
            <td><strong>{{ table.name }}</strong></td>
            <td><code>{{ col.name }}</code></td>
            <td><span class="datatype">{{ col.type }}</span></td>
            <td><span class="datatype">{{ col.datatype }}</span></td>
            <td>
              <span v-if="col.key == 'PRI'" class="key-badge key-primary">PRIMARY KEY</span>
              <span v-else-if="col.key == 'UNI'" class="key-badge key-unique">UNIQUE</span>
              <span v-else-if="col.key == 'MUL'" class="key-badge key-index">INDEXED</span>
              <span v-else class="text-light">-</span>
            </td>
            <td>{{ col.comment }}</td>
          </tr>
        </template>
      </tbody>
    </table>
  </div>

</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
